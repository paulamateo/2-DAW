//LISTAR DIVS
let divs = document.getElementsByTagName('div');
console.log(divs);

//LISTAS TODOS LOS ELEMENTOS CON STYLE == 'BUTTONS'
let div_buttons = document.getElementsByClassName("buttons");
console.log(div_buttons);

//LISTAR 1 DIV CON CLASS == BUTTONS
let div_buttons2 = document.querySelectorAll(".buttons");
console.log(div_buttons2);

//LISTAR TODOS LOS DIVS CON CLASS == BUTTONS





let divs_buttons = document.querySelectorAll('div.buttons');
console.log(divs_buttons);